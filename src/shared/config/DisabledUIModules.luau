--!nolint LocalUnused
--# selene: allow(unused_variable)

local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ! Pamiętaj, nie używaj require! Referujemy obiekt ! --

-- local TEMPLATE: ModuleScript = ReplicatedStorage.shared.ui.TEMPLATE

-- ! Pamiętaj, nie używaj require! Referujemy obiekt ! --

return {
	-- TEMPLATE,
}
